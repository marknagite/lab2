#include <iostream>    
#include <cstring>
using namespace std;
int main() {
	setlocale(LC_ALL, "RU");
	int s = 0;

	char str1[100];
	cin.getline(str1, 100);
	int len = strlen(str1);
	
	for (int i = 0; i < len; i++) {
		str1[i] = tolower(str1[i]);
	}

	cout << str1;
	return 0;
}
