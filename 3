#include <iostream>      
#include <cstring>
using namespace std;
int main() {
	setlocale(LC_ALL, "RU");
	int s = 0;

	char str1[100];
	cin.getline(str1, 100);
	int len = strlen(str1);
	for (int i = len - 1; i >= 0; i--) {
		if (tolower(str1[i]) == 'a') {
			s++;
		}
		else if (!isalpha(str1[i])) {
			break;
		}
	}
	

	cout << s;
	return 0;
}
